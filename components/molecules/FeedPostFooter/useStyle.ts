import { makeStyles } from "tss-react/mui";

const useFeedPostFooter = makeStyles()((theme) => ({
    spanLike:{
        color:theme.palette.baseColor,
    },
    likeIcon:{
        color:theme.palette.baseColor,
    },
}));
export default useFeedPostFooter;
