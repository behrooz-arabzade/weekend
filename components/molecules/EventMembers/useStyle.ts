import { makeStyles } from "tss-react/mui";

const useEventMembers = makeStyles()((theme) => ({

    members: {
        margin: "20px 0px",
        padding: "10px",
        width: "300px"
    }

}));

export default useEventMembers;