import { makeStyles } from "tss-react/mui";

const useReactionButton = makeStyles()((theme) => ({
    root: {
        padding: "5px"
    },
    listIcons:{
        display:"flex"
    }
}));
export default useReactionButton;
